scrollWrapper = document.getElementsByClassName('scroll-wrapper');

scrollWrapper.addEventListener('scroll', () => ({

}));

scrollWrapper.addEventListener('scroll', (event) => {
    const scrollWrapper = document.getElementsByClassName('scroll-wrapper');
    scrollWrapper.classList.add('fadeOut') = 0;
});